import StartGame from './game/main';

// document.addEventListener('resize', () => { StartGame('game-container') })
document.addEventListener('DOMContentLoaded', () => {

    StartGame('game-container');

});